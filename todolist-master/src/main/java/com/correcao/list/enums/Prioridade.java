package com.correcao.list.enums;

public enum Prioridade {
	
	BAIXO,
	MEDIA,
	ALTA

}
//A enumeração (`enum`) é usada para definir um conjunto fixo de constantes nomeadas. No seu caso, `Prioridade` garante que uma tarefa só possa ter um dos três valores predefinidos (`BAIXO`, `MEDIA`, `ALTA`). Isso melhora a legibilidade do código e evita erros com valores inválidos.